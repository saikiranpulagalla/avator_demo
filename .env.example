# Copy this file to `.env` and fill in the values. Do NOT commit your real
# `.env` file to source control. This repository includes `.env.example` so
# you can see which environment variables are required for local development.

# HeyGen API key (used by `scripts/test-heygen.js`)
HEYGEN_API_KEY=

# HeyGen Avatar ID (e.g., "sarah_avatar_001" or from your HeyGen dashboard)
# Get this from: https://app.heygen.com/avatars
HEYGEN_AVATAR_ID=

# HeyGen Voice ID (e.g., "en-US-amy" or from your HeyGen dashboard)
# Get this from: https://app.heygen.com/voices
HEYGEN_VOICE_ID=

# Google Gemini API key (LLM extraction)
GEMINI_API_KEY=

# Google Sheets - target spreadsheet id (used in n8n workflow)
GOOGLE_SHEET_ID=

# n8n base URL (optional): your n8n instance base URL
N8N_BASE_URL=

# Webhook URL where the frontend will POST transcripts (use local webhook validator for dev)
WEBHOOK_URL=http://localhost:5679/webhook/avatar-transcript

# Local webhook validator port (default 5679)
PORT=5679

# Frontend host used for local testing (where `index.html` is served)
FRONTEND_HOST=http://localhost:5173

# Notes:
# - After copying this file to `.env`, fill in the values and then run the
#   Node scripts or start the frontend.
# - Never store production secrets in the frontend; keep them on the backend
#   or in n8n credentials.
